import React from "react";

const Lightbox = ({ color, index }) => {
  return (
    <div
      style={{ backgroundColor: color, padding: 20, margin: 10, fontSize: 30 }}
    >
      Lightbox
    </div>
  );
};

export default Lightbox;
